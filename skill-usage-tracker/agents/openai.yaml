interface:
  display_name: "Data 层"
  short_description: "记录 skill 调用数据,统计高频/慢/失败率,产出优化建议。纯记录不阻塞执行"
  default_prompt: "Use $skill-usage-tracker to record skill invocations or query usage statistics (call counts, durations, failure rates, optimization suggestions). Records are non-blocking and stored locally."
