# runtime.yaml — code-review
# 由 skill-runtime 定义契约，validate_runtime.py 校验
timeout: 180
retry:
  max: 0
  backoff: fixed
  interval: 5
outputs:
  - path: code-review-report.md
    type: file
  - path: code-review-report.json
    type: file
