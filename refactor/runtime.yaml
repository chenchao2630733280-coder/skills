# runtime.yaml — refactor
# 由 skill-runtime 定义契约，validate_runtime.py 校验
timeout: 600
retry:
  max: 1
  backoff: fixed
  interval: 10
outputs:
  - path: refactor-plan.md
    type: file
  - path: refactor-report.md
    type: file
  - path: refactor-report.json
    type: file
